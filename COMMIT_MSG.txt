Add login validation tests with POM

- Added LoginPage methods: openLoginForm(), submitWithBypass(), expectError()
- Created tta-login-test.spec.ts with 6 login validation scenarios
  - Invalid credentials, blank username, blank password, both blank
  - Locked out user, successful login redirect
- Fixed LoginPage.goto() to use baseURL from config
- Cleaned up exploration test file

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
